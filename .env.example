# 🔑 CONFIGURATION ENVIRONNEMENT BACKEND
# Copier ce fichier vers .env et remplir avec vos vraies valeurs

# Base de données MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database-name

# Clé secrète JWT (minimum 32 caractères)
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-chars

# Durée d'expiration JWT
JWT_EXPIRES_IN=7d

# Port du serveur
PORT=8080

# Environnement (development, production, test)
NODE_ENV=development

# INSTRUCTIONS:
# 1. Renommer ce fichier en .env
# 2. Remplacer toutes les valeurs par vos vraies données
# 3. Ne JAMAIS commiter le fichier .env dans Git
# 4. Pour la production, utiliser des variables d'environnement sécurisées
